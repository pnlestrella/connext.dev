import { Text } from 'react-native';
import { SafeAreaView } from 'react-native-safe-area-context';

export const MessageScreenEmployer= () => {
  return (
    <SafeAreaView>
      <Text>This is Employers Message</Text>
      
    </SafeAreaView>
  );
};
