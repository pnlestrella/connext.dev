import { Text } from 'react-native';
import { SafeAreaView } from 'react-native-safe-area-context';

export const MessageScreenJS = () => {
  return (
    <SafeAreaView>
      <Text>This is Job seekers Message</Text>
      
    </SafeAreaView>
  );
};
